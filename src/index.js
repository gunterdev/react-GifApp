import React from 'react';
import ReactDOM from 'react-dom';
import GiphifyApp from './GiphifyApp'
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <GiphifyApp />
  </React.StrictMode>,
  document.getElementById('root')
);
